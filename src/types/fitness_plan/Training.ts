export interface ITraining {
    id: number;
    title: string;
    training_type: string;
    description: string;
}

export interface TrainingForm {
    title: string;
    training_type: string;
    description: string;
}