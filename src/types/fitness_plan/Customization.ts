export interface ICustomization {
    id: number;
    reps: number;
    sets: number;
    duration: number;
}

export interface CustomizationForm {
    reps: number;
    sets: number;
    duration: number;
}